{
  "name": "ai-router",
  "version": "0.1.0",
  "description": "Router service for context compression and model routing",
  "main": "src/server.js",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test",
    "prepare": "git -C .. config core.hooksPath .githooks"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^4.19.2"
  }
}
